{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "webAppName": {
            "value": "#{Azure:WebApp:Name}"
        },
        "appServiceName": {
            "value": "#{Azure:AppService:Name}"
        },
        "appServiceSkuName": {
            "value": "#{Azure:AppService:SkuName}"
        }
    }
}